machine:
#  python:
#    version: 2.7.13
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild-26 || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild-26 /bin/sh urpms -g UnitedRPMs/vlc  -s vlc.spec -r true -d 'dist .fc26' -c true:
        parallel: true
        timeout: 9600

        


