machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild /bin/sh urpms -g UnitedRPMs/kodi -s kodi.spec -r true -d 'dist .fc25':
        parallel: true
        timeout: 9600

        


