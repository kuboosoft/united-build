machine:
  services:
    - docker

python:
    version: 2.7.3

test:
  override:
    - docker run --privileged=true -v $(pwd):/home/root/build davidva/unitedrpms /bin/sh urpms -g UnitedRPMs/ffmpeg -s ffmpeg.spec -t fedora-rawhide-x86_64:
        parallel: true
        timeout: 3600

