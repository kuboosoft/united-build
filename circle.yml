machine:
  services:
    - docker

test:
  override:
- sudo docker run --cap-add=SYS_ADMIN --privileged=true -v $(pwd):/home/root/build davidva/unitedrpms /bin/sh urpms -g UnitedRPMs/ffmpeg -s ffmpeg.spec -t fedora-24-x86_64:
        parallel: true
        timeout: 3600

#

