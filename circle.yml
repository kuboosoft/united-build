machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t davidva/unitedrpms .

test:
  override:
- docker run --cap-add=SYS_ADMIN -i -t davidva/unitedrpms /bin/bash
- /urpms -g UnitedRPMs/ffmpeg -s ffmpeg.spec -t fedora-rawhide-x86_64
        
#

