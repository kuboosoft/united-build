machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild || true

test:
  override:
#    - docker run --privileged=true --cap-add=SYS_ADMIN -v $(pwd):/root/enjoy davidva/unibuild /bin/sh urpms -g UnitedRPMs/chromium-freeworld -s chromium.spec -r true -d 'dist .fc25':
- docker run --privileged=true --cap-add=SYS_ADMIN -v $(pwd):/root/$HOME davidva/usync /bin/sh git clone https://github.com/kuboosoft/united-build.git && cd united-build \
&& chmod a+x sync-united \
&& ./sync-united:
        parallel: true
        timeout: 9600

        


