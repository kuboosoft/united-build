machine:
  services:
    - docker

# the build scripts
script:
- docker run --privileged=true -v $(pwd):/home/root/build davidva/unitedrpms /bin/sh urpms -g UnitedRPMs/ffmpeg -s ffmpeg.spec -t fedora-26-x86_64
