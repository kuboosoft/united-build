machine:
#  python:
#    version: 2.7.13
  services:
    - docker

dependencies:
  pre:
    - sudo docker pull davidva/unibuild-24 || true

test:
  override:
    - docker run -v $(pwd):/root/enjoy davidva/unibuild-24 /bin/sh urpms -g UnitedRPMs/opera  -s opera.spec -r true -d 'dist .fc24' -c true:
        parallel: true
        timeout: 9600

        


