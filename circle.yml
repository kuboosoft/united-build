machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
- docker run --cap-add=SYS_ADMIN -i -t davidva/unitedrpms /bin/bash
    - ./unitedrpms /bin/sh urpms -g UnitedRPMs/ffmpeg -s ffmpeg.spec -t fedora-rawhide-x86_64:
        parallel: true
        timeout: 3600

#

