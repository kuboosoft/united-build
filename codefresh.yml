version: '1.0'

steps:

  unibuild-27:
    type: build
    description: UC - build step
    image-name: davidva/unibuild-27
    dockerfile: Dockerfile
    tag: latest
    no_cache: true
#    tag: ${{CF_BRANCH}}
#    privileged: true
