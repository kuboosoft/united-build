version: '1.0'

steps:

  build-prj:
    type: build
    description: UC - build step
    image-name: davidva/unibuild-27
    dockerfile: Dockerfile
    tag: latest
#    tag: ${{CF_BRANCH}}
#    privileged: true
