version: '1.0'

steps:

  urpms:
    type: build
    description: UC - build step
    image-name: davidva/unibuild-27
    dockerfile: Dockerfile
#    tag: master
    no_cache: true
    tag: ${{CF_BRANCH}}
#    privileged: true
    restart: always
