# Build Environment

language: python

python:
  - 2.7

install: true

build:
  pre_ci_boot:
    image_name: davidva/unitedrpms
    image_tag: latest
    pull: true
    env: FOO=bar
    options: "-e HOME=/root"

# commands in this section run inside your CI container. In addition, any commands such as ls, in this section can be used to skip default processing for CI.
  ci:
    # Here you'd place anything you want to test. For PHP apps, maybe Phpunit?
    - /urpms -g UnitedRPMs/vlc -s vlc.spec -t fedora-24-x86_64

# the build scripts
#script:
#- docker run --privileged=true -v $(pwd):/root/build/MOCK davidva/unitedrpms /bin/sh urpms -g UnitedRPMs/vlc -s vlc.spec -t fedora-24-x86_64

notifications:
email: davidva@tutanota.com
# slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj
##


